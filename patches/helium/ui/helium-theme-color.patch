--- a/ui/color/color_provider_key.h
+++ b/ui/color/color_provider_key.h
@@ -135,6 +135,7 @@ struct COMPONENT_EXPORT(COLOR_PROVIDER_K
   FrameStyle frame_style = FrameStyle::kDefault;
   UserColorSource user_color_source = UserColorSource::kAccent;
   std::optional<SkColor> user_color = std::nullopt;
+  std::optional<SkColor> helium_theme_color = std::nullopt;
   std::optional<SchemeVariant> scheme_variant = std::nullopt;
   scoped_refptr<ThemeInitializerSupplier> custom_theme = nullptr;
   // Only dereferenced when populating the ColorMixer. After that, used to
@@ -147,12 +148,13 @@ struct COMPONENT_EXPORT(COLOR_PROVIDER_K
     auto* rhs_app_controller = other.app_controller.get();
     return std::tie(color_mode, contrast_mode, forced_colors, system_theme,
                     frame_type, frame_style, user_color_source, user_color,
-                    scheme_variant, custom_theme, lhs_app_controller) <
+                    helium_theme_color, scheme_variant, custom_theme,
+                    lhs_app_controller) <
            std::tie(other.color_mode, other.contrast_mode, other.forced_colors,
                     other.system_theme, other.frame_type, other.frame_style,
                     other.user_color_source, other.user_color,
-                    other.scheme_variant, other.custom_theme,
-                    rhs_app_controller);
+                    other.helium_theme_color, other.scheme_variant,
+                    other.custom_theme, rhs_app_controller);
   }
 };
 
--- a/chrome/browser/ui/views/frame/browser_widget.h
+++ b/chrome/browser/ui/views/frame/browser_widget.h
@@ -5,11 +5,14 @@
 #ifndef CHROME_BROWSER_UI_VIEWS_FRAME_BROWSER_WIDGET_H_
 #define CHROME_BROWSER_UI_VIEWS_FRAME_BROWSER_WIDGET_H_
 
+#include <optional>
+
 #include "base/memory/raw_ptr.h"
 #include "build/build_config.h"
 #include "content/public/browser/keyboard_event_processing_result.h"
+#include "third_party/skia/include/core/SkColor.h"
 #include "ui/base/mojom/menu_source_type.mojom-forward.h"
 #include "ui/base/mojom/window_show_state.mojom-forward.h"
 #include "ui/base/pointer/touch_ui_controller.h"
 #include "ui/views/context_menu_controller.h"
 #include "ui/views/widget/widget.h"
@@ -66,6 +69,8 @@ class BrowserWidget : public views::Widg
 
   ~BrowserWidget() override;
 
+  void SetHeliumThemeColor(std::optional<SkColor> color);
+
 #if BUILDFLAG(IS_LINUX)
   // Returns whether the frame is in a tiled state.
   bool tiled() const { return tiled_; }
@@ -171,6 +176,8 @@ class BrowserWidget : public views::Widg
   // The BrowserView is our ClientView. This is a pointer to it.
   raw_ptr<BrowserView> browser_view_;
 
+  std::optional<SkColor> helium_theme_color_;
+
   // Cache theme_service_ if feature BrowserWidgetCacheThemeService is enabled.
   raw_ptr<ThemeService> theme_service_;
 
--- a/chrome/browser/ui/views/frame/browser_widget.cc
+++ b/chrome/browser/ui/views/frame/browser_widget.cc
@@ -149,6 +149,15 @@ BrowserWidget::~BrowserWidget() {
   browser_view_->browser()->GetFeatures().TearDownPreBrowserWindowDestruction();
 }
 
+void BrowserWidget::SetHeliumThemeColor(std::optional<SkColor> color) {
+  if (helium_theme_color_ == color) {
+    return;
+  }
+
+  helium_theme_color_ = color;
+  UserChangedTheme(BrowserThemeChangeType::kWebAppTheme);
+}
+
 void BrowserWidget::InitBrowserWidget() {
   browser_native_widget_ =
       BrowserNativeWidgetFactory::CreateBrowserNativeWidget(this,
@@ -448,6 +457,10 @@ ui::ColorProviderKey BrowserWidget::GetC
 
   key.app_controller = browser_view_->browser()->app_controller();
 
+  if (!IsIncognitoBrowser()) {
+    key.helium_theme_color = helium_theme_color_;
+  }
+
 #if BUILDFLAG(IS_CHROMEOS)
   // ChromeOS SystemWebApps use the OS theme all the time.
   if (ash::IsSystemWebApp(browser_view_->browser())) {
--- a/chrome/browser/ui/views/frame/browser_view.h
+++ b/chrome/browser/ui/views/frame/browser_view.h
@@ -786,6 +786,8 @@ class BrowserView : public BrowserWindow
 
   // content::WebContentsObserver:
   void TitleWasSet(content::NavigationEntry* entry) override;
+  void PrimaryPageChanged(content::Page& page) override;
+  void DidChangeThemeColor() override;
 
   // views::ClientView:
   views::CloseRequestResult OnWindowCloseRequested() override;
@@ -1202,6 +1204,7 @@ class BrowserView : public BrowserWindow
 
   // Called by BrowserWindowThemeObserver when the theme changes.
   void UserChangedTheme(BrowserThemeChangeType theme_change_type);
+  void UpdateHeliumThemeColor();
 
   void UpdateAccessibleNameForRootView();
   void UpdateAccessibleURLForRootView(const GURL& url);
--- a/chrome/browser/ui/views/frame/browser_view.cc
+++ b/chrome/browser/ui/views/frame/browser_view.cc
@@ -37,7 +37,8 @@
 #include "base/task/sequenced_task_runner.h"
 #include "base/task/single_thread_task_runner.h"
 #include "base/trace_event/trace_event.h"
 #include "build/build_config.h"
+#include "third_party/skia/include/core/SkColor.h"
 #include "chrome/app/chrome_command_ids.h"
 #include "chrome/browser/actor/ui/actor_overlay_web_view.h"
 #include "chrome/browser/app_mode/app_mode_utils.h"
@@ -2580,6 +2581,7 @@ void BrowserView::OnActiveTabChanged(con
   }
 
   WebContentsObserver::Observe(new_contents);
+  UpdateHeliumThemeColor();
 
   // If |contents_container_| already has the correct WebContents, we can save
   // some work.  This also prevents extra events from being reported by the
@@ -3652,6 +3654,26 @@ void BrowserView::TitleWasSet(content::N
   UpdateAccessibleNameForRootView();
 }
 
+void BrowserView::PrimaryPageChanged(content::Page& /*page*/) {
+  UpdateHeliumThemeColor();
+}
+
+void BrowserView::DidChangeThemeColor() {
+  UpdateHeliumThemeColor();
+}
+
+void BrowserView::UpdateHeliumThemeColor() {
+  std::optional<SkColor> color;
+  if (content::WebContents* web_contents = GetActiveWebContents()) {
+    color = web_contents->GetThemeColor();
+    if (color) {
+      color = SkColorSetA(*color, SK_AlphaOPAQUE);
+    }
+  }
+
+  browser_widget()->SetHeliumThemeColor(color);
+}
+
 void BrowserView::TouchModeChanged() {
 #if BUILDFLAG(IS_CHROMEOS)
   // Reparenting is unnecessary when kWebUITabStrip is enabled because ChromeOS
--- a/chrome/browser/ui/color/chrome_color_mixer.cc
+++ b/chrome/browser/ui/color/chrome_color_mixer.cc
@@ -840,7 +840,45 @@ void AddChromeColorMixer(ui::ColorProvid
   mixer[kColorToolbarSeparator] = {kColorToolbarSeparatorDefault};
   mixer[kColorToolbarActionItemEngaged] = {
       ui::PickGoogleColor(ui::kColorSysPrimary, kColorToolbar,
                           color_utils::kMinimumVisibleContrastRatio)};
+  if (key.helium_theme_color) {
+    const SkColor theme_color =
+        SkColorSetA(*key.helium_theme_color, SK_AlphaOPAQUE);
+    const SkColor foreground_color =
+        color_utils::GetColorWithMaxContrast(theme_color);
+    const bool use_light_foreground = foreground_color == SK_ColorWHITE;
+    const SkColor location_bar_overlay =
+        use_light_foreground ? SkColorSetA(SK_ColorWHITE, 0x1F)
+                             : SkColorSetA(SK_ColorBLACK, 0x14);
+    const SkColor location_bar_background =
+        color_utils::GetResultingPaintColor(location_bar_overlay, theme_color);
+    const SkColor inactive_foreground = SkColorSetA(foreground_color, 0xB2);
+
+    mixer[kColorToolbar] = {theme_color};
+    mixer[kColorLocationBarBackground] = {location_bar_background};
+    mixer[kColorLocationBarBackgroundHovered] =
+        ui::GetResultingPaintColor(ui::kColorSysStateHoverBrightBlendProtection,
+                                   kColorLocationBarBackground);
+    mixer[kColorToolbarButtonText] = {foreground_color};
+    mixer[kColorToolbarButtonIcon] = {foreground_color};
+    mixer[kColorToolbarButtonIconHovered] = {foreground_color};
+    mixer[kColorToolbarButtonIconPressed] = {foreground_color};
+    mixer[kColorToolbarText] = {foreground_color};
+    mixer[kColorToolbarContentAreaSeparator] =
+        ui::AlphaBlend(kColorToolbarButtonIcon, kColorToolbar, 0x3A);
+
+    mixer[kColorTabBackgroundActiveFrameActive] = {
+        kColorLocationBarBackground};
+    mixer[kColorTabBackgroundActiveFrameInactive] = {
+        kColorLocationBarBackground};
+    mixer[kColorTabForegroundActiveFrameActive] = {foreground_color};
+    mixer[kColorTabForegroundActiveFrameInactive] = {foreground_color};
+    mixer[kColorTabForegroundInactiveFrameActive] = {inactive_foreground};
+    mixer[kColorTabForegroundInactiveFrameInactive] = {inactive_foreground};
+
+    mixer[kColorFrameCaptionActive] = {foreground_color};
+    mixer[kColorFrameCaptionInactive] = {foreground_color};
+  }
   mixer[kColorToolbarSeparatorDefault] =
       ui::SetAlpha(kColorToolbarButtonIcon, 0x4D);
   mixer[kColorToolbarText] = {kColorToolbarTextDefault};
